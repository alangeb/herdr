#!/bin/sh
# HERDR_INTEGRATION_ID=tau
# HERDR_INTEGRATION_VERSION=1
d="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
[ -f "$d/tau_entry.py" ] || d="$HOME/.herdr/tau"
exec /usr/bin/python3 "$d/tau_entry.py" "$@"
